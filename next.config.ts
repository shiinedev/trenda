import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
      domains: ['plus.unsplash.com',"images.pexels.com","36oib8nepu.ufs.sh"],
  },
};

export default nextConfig;
